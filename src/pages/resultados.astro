---
// Página unificada en /pronosticos ("La Polla"): resultados + pronósticos de
// todos en un solo lugar. Esta ruta se mantiene como redirección para no romper
// enlaces viejos (bookmarks, mensajes de WhatsApp ya enviados).
return Astro.redirect('/pronosticos');
---
